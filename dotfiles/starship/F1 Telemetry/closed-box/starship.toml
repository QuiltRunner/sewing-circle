# ========== F1 VERTICAL TELEMETRY TOWER  ==========

format = """
[┌──────────────────────────┐](fg:#d6b3ff)
[│ 🏁 F1 • TELEMETRY PANEL  │](fg:#d6b3ff)
[│ $os          │](fg:#d6b3ff)
[│ $hostname            │](fg:#d6b3ff)
[│ $directory               │](fg:#d6b3ff)
[│ $memory_usage         │](fg:#d6b3ff)
[│ PIT: $git_branch$git_status                    │](fg:#d6b3ff)
[│ $cmd_duration        │](fg:#d6b3ff)
[│ $time             │](fg:#d6b3ff)
[└──────────────────────────┘](fg:#d6b3ff)
$character
"""

# ─────────────────────────────
# CHARACTER (PROMPT CURSOR)
# ─────────────────────────────
[character]
success_symbol = "[➤](bold fg:#b3f0ff)"
error_symbol   = "[➤](bold fg:#ff79d1)"
vicmd_symbol   = "[V](fg:#d6b3ff)"

# ─────────────────────────────
# OS MODULE = CHASSIS
# ─────────────────────────────
[os]
disabled = false
format = "[CHASSIS: $name]($style)"
style = "fg:#ffd1f7"

# ─────────────────────────────
# HOSTNAME = CAR NUMBER
# ─────────────────────────────
[hostname]
disabled = false
ssh_only = false
format = "[CAR #: $hostname]($style)"
style = "fg:#b3f0ff"

# ─────────────────────────────
# DIRECTORY = TRACK POSITION
# ─────────────────────────────
[directory]
style = "fg:#b3f0ff"
format = "[TRACK: $path]($style)"
home_symbol = '~🏆'

# ─────────────────────────────
# MEMORY = ENGINE LOAD
# ─────────────────────────────
[memory_usage]
disabled = false
threshold = 0
style = "fg:#ff9ce6"
symbol = "🔥"
format = "[ENGINE LOAD: $ram_pct]($style)"

# ─────────────────────────────
# GIT = PIT DATA
# ─────────────────────────────
[git_branch]
symbol = "🚥"
style = "fg:#c4b5fd"
format = "[$symbol$branch]($style) "

# ─────────────────────────────
# CMD DURATION = LAP TIME
# ─────────────────────────────
[cmd_duration]
min_time = 0
style = "fg:#b3f0ff"
format = "[LAP TIME: $duration]($style)"
show_milliseconds = true

# ─────────────────────────────
# TIME = SESSION CLOCK
# ─────────────────────────────
[time]
disabled = false
use_12hr = true
time_format = "%H:%M"
style = "fg:#ffd1f7"
format = "[CLOCK: $time]($style)"
